name: Revert Staging Code

on:
  workflow_dispatch:
    inputs:
      commit_sha:
        description: 'Commit SHA or tag to revert to (leave empty to revert last commit)'
        required: false
        type: string
      create_revert_commit:
        description: 'Create a revert commit (true) or reset hard (false)'
        required: true
        type: boolean
        default: true

jobs:
  revert:
    name: Revert Staging Code
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      - name: Verify PAT_TOKEN exists
        run: |
          if [ -z "${{ secrets.PAT_TOKEN }}" ]; then
            echo "❌ PAT_TOKEN secret is not configured!"
            echo ""
            echo "This workflow requires a Personal Access Token (PAT) to push changes."
            echo "The default GITHUB_TOKEN doesn't have write permissions for this repository."
            echo ""
            echo "To fix this:"
            echo "1. Go to: https://github.com/settings/tokens"
            echo "2. Click 'Generate new token (classic)'"
            echo "3. Select the 'repo' scope (full control of private repositories)"
            echo "4. Copy the generated token"
            echo "5. Go to your repository: Settings → Secrets and variables → Actions"
            echo "6. Add a new secret named 'PAT_TOKEN' with the token value"
            echo ""
            exit 1
          else
            echo "✅ PAT_TOKEN is configured"
          fi
      
      - name: Checkout code
        uses: actions/checkout@v3
        with:
          ref: staging-ismail-admin-panel
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN2 }}
          persist-credentials: true
      
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
      
      - name: Revert to previous code
        id: revert
        run: |
          if [ "${{ github.event.inputs.create_revert_commit }}" = "true" ]; then
            echo "🔄 Creating revert commit for staging..."
            
            if [ -z "${{ github.event.inputs.commit_sha }}" ]; then
              # Revert the last commit
              LAST_COMMIT=$(git rev-parse HEAD)
              git revert --no-edit HEAD
              echo "✅ Reverted commit: $LAST_COMMIT"
            else
              # Revert to specific commit
              COMMIT_SHA="${{ github.event.inputs.commit_sha }}"
              
              # Check if it's a tag
              if git rev-parse "$COMMIT_SHA" >/dev/null 2>&1; then
                echo "📍 Found commit/tag: $COMMIT_SHA"
                
                # Get all commits between current HEAD and the target commit
                CURRENT_SHA=$(git rev-parse HEAD)
                TARGET_SHA=$(git rev-parse "$COMMIT_SHA")
                
                if [ "$CURRENT_SHA" = "$TARGET_SHA" ]; then
                  echo "⚠️ Already at target commit. Nothing to revert."
                  exit 0
                fi
                
                # Check if target is an ancestor
                if git merge-base --is-ancestor "$TARGET_SHA" "$CURRENT_SHA"; then
                  # Target is in history, revert commits between them
                  COMMITS_TO_REVERT=$(git rev-list --reverse "$TARGET_SHA"..HEAD)
                  for commit in $COMMITS_TO_REVERT; do
                    git revert --no-edit "$commit" || {
                      echo "⚠️ Could not revert commit $commit (might have conflicts)"
                      git revert --abort 2>/dev/null || true
                    }
                  done
                  echo "✅ Reverted to commit: $TARGET_SHA"
                else
                  echo "❌ Target commit is not in current branch history"
                  exit 1
                fi
              else
                echo "❌ Commit or tag not found: $COMMIT_SHA"
                exit 1
              fi
            fi
          else
            echo "🔄 Performing hard reset on staging..."
            
            if [ -z "${{ github.event.inputs.commit_sha }}" ]; then
              # Reset to previous commit
              git reset --hard HEAD~1
              echo "✅ Reset to previous commit"
            else
              # Reset to specific commit
              COMMIT_SHA="${{ github.event.inputs.commit_sha }}"
              
              if git rev-parse "$COMMIT_SHA" >/dev/null 2>&1; then
                git reset --hard "$COMMIT_SHA"
                echo "✅ Reset to commit: $COMMIT_SHA"
              else
                echo "❌ Commit or tag not found: $COMMIT_SHA"
                exit 1
              fi
            fi
          fi
      
      - name: Push changes
        if: steps.revert.outcome == 'success'
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN2 }}
        run: |
          # Use PAT_TOKEN for authentication (verified in earlier step)
          echo "🔑 Using Personal Access Token (PAT_TOKEN2) for authentication"
          
          # Configure remote URL with token for authentication
          git remote set-url origin https://x-access-token:${PAT_TOKEN2}@github.com/${GITHUB_REPOSITORY}.git
          
          if [ "${{ github.event.inputs.create_revert_commit }}" = "true" ]; then
            echo "📤 Pushing revert commit to staging..."
            if git push origin "HEAD:staging-ismail-admin-panel"; then
              echo "✅ Staging revert completed successfully!"
            else
              echo "❌ Push failed. This usually means:"
              echo "   1. The GITHUB_TOKEN doesn't have write permissions"
              echo "   2. Repository workflow permissions are set to read-only"
              echo "   3. You need to add a PAT_TOKEN secret with 'repo' scope"
              echo ""
              echo "To fix:"
              echo "   1. Go to Settings → Secrets and variables → Actions"
              echo "   2. Add a new secret named 'PAT_TOKEN'"
              echo "   3. Use a Personal Access Token with 'repo' scope"
              exit 1
            fi
          else
            echo "📤 Force pushing reset to staging..."
            if git push origin "HEAD:staging-ismail-admin-panel" --force; then
              echo "✅ Staging revert completed successfully!"
            else
              echo "❌ Force push failed. See error message above for details."
              exit 1
            fi
          fi
      
      - name: Create summary
        if: always()
        run: |
          echo "## Staging Revert Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch**: staging-ismail-admin-panel (Staging)" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit SHA**: ${{ github.event.inputs.commit_sha || 'Last commit' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Method**: ${{ github.event.inputs.create_revert_commit == 'true' && 'Revert commit' || 'Hard reset' }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ steps.revert.outcome }}" >> $GITHUB_STEP_SUMMARY
